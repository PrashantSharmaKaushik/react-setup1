import types from '../types';

let initialState = {

}

export default function (state = initialState, action) {
    switch (action.default) {
        default: {
            return state;
        }
    }
}